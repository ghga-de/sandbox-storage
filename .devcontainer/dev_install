#!/bin/bash
# install service in dev container

cd /workspace

pip install \
    -e \ # install in edit mode
    --use-feature=in-tree-build \ # use future way of installing packages
    ".[testing]" # install with testing dependencies
